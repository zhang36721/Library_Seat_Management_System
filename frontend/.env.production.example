# Cloud deployment path. This avoids conflict with the existing project on
# https://www.kento.top/ and its /api routes.
VITE_PUBLIC_BASE_PATH=/library-seat/
VITE_API_BASE_URL=/library-seat

# If you deploy frontend and API on different domains, set for example:
# VITE_API_BASE_URL=https://api.kento.top
