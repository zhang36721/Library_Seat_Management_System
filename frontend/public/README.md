# 静态资源

本目录用于存放前端项目的静态资源文件，这些文件在构建时会被直接复制到输出目录。

## 待添加的文件

- `favicon.ico` - 网站图标
- `logo.svg` - 项目 Logo
- `manifest.json` - PWA 清单文件（可选）

## 使用说明

1. 将不需要构建工具处理的静态文件放在此目录
2. 在代码中通过绝对路径引用这些文件
3. 构建时这些文件会被原样复制到 dist 目录