# v0.6.1 主控接线布局规划

本文档用于规划主控 STM32F103C8T6 的接线布局。v0.6.1 只做硬件层整理和验收辅助，不新增外设驱动，不接后端、前端、ESP32 上传或 ZigBee 业务协议。

## 1. 引脚分配总表

| 功能模块 | STM32 引脚 | 接线规划 | 当前状态 | 备注 |
|----------|------------|----------|----------|------|
| MCU | STM32F103C8T6 | 主控最小系统板 | 已确认 | Flash 64KB，最后 1KB 页用于 boot count |
| 运行 LED | PC13 | 板载/外接 LED | 已初始化 | 低电平点亮，默认高电平关闭 |
| 用户按键 | PA0 | 独立按键输入 | 已初始化，待实测 | 当前按上拉、低电平按下处理 |
| 蜂鸣器 | PA4 | 有源蜂鸣器信号脚 | 已初始化，待实测 | 当前按高电平响处理；与 RC522 NSS 冲突待确认 |
| USART1_TX | PA9 | ZigBee RXD | 预留 | 不接入 v0.6.1 debug 协议 |
| USART1_RX | PA10 | ZigBee TXD | 预留 | 不接入 v0.6.1 debug 协议 |
| USART2_TX | PA2 | USB-TTL RXD | 已初始化 | Debug 输出，115200 |
| USART2_RX | PA3 | USB-TTL TXD | 已初始化 | Debug 输入，`FF CMD DATA FF` |
| USART3_TX | PB10 | ESP32S3 RX | 预留 | ESP32S3 后续独立串口 |
| USART3_RX | PB11 | ESP32S3 TX | 预留 | 不与 USART2 debug 共用 |
| 座位检测 1 | 待确认 | 红外/压力检测输出 | 宏已预留 | 未确认前不占用具体 GPIO |
| 座位检测 2 | 待确认 | 红外/压力检测输出 | 宏已预留 | 未确认前不占用具体 GPIO |
| 座位检测 3 | 待确认 | 红外/压力检测输出 | 宏已预留 | 未确认前不占用具体 GPIO |
| RC522 SCK | PA5 或待确认 | SPI 时钟 | 规划中 | 需结合实物接线确认 |
| RC522 MISO | PA6 或待确认 | SPI MISO | 规划中 | 需结合实物接线确认 |
| RC522 MOSI | PA7 或待确认 | SPI MOSI | 规划中 | 需结合实物接线确认 |
| RC522 NSS/SDA | PA4 或待确认 | SPI 片选 | 冲突待确认 | PA4 当前规划为蜂鸣器 |
| RC522 RST | 待确认 | 复位 GPIO | 规划中 | 不在 v0.6.1 实现驱动 |
| OLED SCL | PB6 或待确认 | I2C1_SCL | 规划中 | 当前规划为 I2C OLED |
| OLED SDA | PB7 或待确认 | I2C1_SDA | 规划中 | 当前规划为 I2C OLED |
| DS1302 CLK | 待确认 | GPIO 输出 | 规划中 | 不在 v0.6.1 实现驱动 |
| DS1302 DAT | 待确认 | GPIO 双向 | 规划中 | 不在 v0.6.1 实现驱动 |
| DS1302 RST | 待确认 | GPIO 输出 | 规划中 | 不在 v0.6.1 实现驱动 |
| ULN2003 IN1 | 待确认 | 步进电机控制 | 规划中 | ULN2003/ULN12003 型号需确认 |
| ULN2003 IN2 | 待确认 | 步进电机控制 | 规划中 | 不在 v0.6.1 实现驱动 |
| ULN2003 IN3 | 待确认 | 步进电机控制 | 规划中 | 不在 v0.6.1 实现驱动 |
| ULN2003 IN4 | 待确认 | 步进电机控制 | 规划中 | 不在 v0.6.1 实现驱动 |

## 2. USART 职责

| 串口 | 引脚 | 职责 | v0.6.1 状态 |
|------|------|------|-------------|
| USART1 | PA9 TX / PA10 RX | ZigBee 通信预留 | 未初始化，不跑业务协议 |
| USART2 | PA2 TX / PA3 RX | Debug 调试串口，115200，`FF CMD DATA FF` | 已初始化，当前唯一命令通道 |
| USART3 | PB10 TX / PB11 RX | ESP32S3 通信预留 | 未初始化，不上传数据 |

ESP32S3 暂时不要接 USART2。旧文档里 ESP32S3 占用 PA2/PA3 的描述属于冲突待确认；若实物已经固定，需要后续重新评估 USART 规划。

## 3. 基础 I/O 接线

| 模块 | 信号 | STM32 引脚 | 电平规划 | 默认状态 |
|------|------|------------|----------|----------|
| LED | LED-CTRL | PC13 | 低电平点亮 | 高电平关闭 |
| 按键 | KEY | PA0 | 暂按上拉低有效 | 上拉释放态 |
| 蜂鸣器 | BUZZER | PA4 | 暂按高电平响 | 低电平关闭 |

PA0 和 PA4 的有效电平以实机验收为准。PA4 与 RC522 NSS 可能冲突，必须在接线最终确认前保持“冲突待确认”。

## 4. 3 路座位检测输入预留

| 座位 | STM32 引脚 | 输入类型 | 有效电平 | 当前代码状态 |
|------|------------|----------|----------|--------------|
| Seat 1 | 待确认 | GPIO 输入 | `APP_SEAT_SENSOR_ACTIVE_LEVEL` | 已预留宏，未初始化 |
| Seat 2 | 待确认 | GPIO 输入 | `APP_SEAT_SENSOR_ACTIVE_LEVEL` | 已预留宏，未初始化 |
| Seat 3 | 待确认 | GPIO 输入 | `APP_SEAT_SENSOR_ACTIVE_LEVEL` | 已预留宏，未初始化 |

真实引脚确认前，`APP_SEAT_SENSOR_PINS_CONFIRMED` 保持 0，`FF 30 / FF 31` 只打印简短提示。

## 5. 外设接线规划

| 外设 | 接口规划 | 推荐/待确认引脚 | 当前状态 |
|------|----------|-----------------|----------|
| RC522 | SPI | SCK PA5、MISO PA6、MOSI PA7、NSS PA4 或待确认、RST 待确认 | PA4 冲突待确认 |
| OLED | I2C | SCL PB6、SDA PB7 或待确认 | 明确优先规划为 I2C，不使用 SPI，除非实物变更 |
| DS1302 | GPIO 三线 | CLK/DAT/RST 均待确认 | 只规划，不实现 |
| 步进电机 | ULN2003/ULN12003 GPIO 四线 | IN1-IN4 待确认 | 只规划，不实现 |
| ZigBee | USART1 | PA9/PA10 | 预留 |
| ESP32S3 | USART3 | PB10/PB11 | 预留 |

## 6. 确认清单

已确认：

- 主控芯片为 STM32F103C8T6。
- USART2 用于 debug，115200，协议为 `FF CMD DATA FF`。
- PC13 为运行 LED，低电平点亮。
- PA0 已作为按键输入初始化。
- PA4 已作为蜂鸣器输出初始化。

待确认：

- PA0 按键实际上拉/下拉和有效电平。
- PA4 蜂鸣器实际有效电平。
- 3 路座位检测真实 GPIO。
- RC522、OLED、DS1302、步进电机真实接线。
- ESP32S3 是否已经实物占用 USART2。

冲突待确认：

- PA4 蜂鸣器与 RC522 NSS/SDA 可能冲突。
- 旧 ESP32S3 接线文档使用 PA2/PA3，与 v0.6.1 USART2 debug 规划冲突。
